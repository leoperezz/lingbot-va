# Core PyTorch - install separately with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# Vision and Diffusion
diffusers>=0.27.2
transformers>=4.30.0
tokenizers>=0.13.0

# Core utilities
numpy>=1.24.0,<2.3.0
einops>=0.7.0
tqdm>=4.60.0
imageio>=2.30.0
pillow>=9.5.0

# Model acceleration
accelerate>=0.20.0

# Websocket for server/client
websockets>=12.0
msgpack>=1.0.0

# Image processing
opencv-python>=4.8.0
matplotlib>=3.7.0

# Additional utilities
easydict>=1.10
ftfy>=6.1.0
psutil>=5.9.0
packaging>=23.0
pyyaml>=6.0.0
omegaconf>=2.3.0
huggingface-hub>=0.20.0

# Optional: Install separately
# pip install "lerobot @ git+https://github.com/huggingface/lerobot.git"
# flash-attn>=2.0.0  # Install with: pip install flash-attn --no-build-isolation